{
  "name": "jinsei-server",
  "version": "1.0.0",
  "description": "Backend proxy for 人生 (JINSEI) — holds the Anthropic API key server-side and streams responses to the browser.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
